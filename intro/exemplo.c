#include <stdio.h>

int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!");

   for (int number = 5; number <= 10; number++){
      printf(number);
   }



   return 0;
}